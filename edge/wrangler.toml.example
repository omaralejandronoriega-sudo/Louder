name = "louder-artistas"
main = "cloudflare-worker.js"
compatibility_date = "2026-09-25"

# Keep the existing WordPress origin for all other URLs.
# Apply this Worker only to the Artistas path.
#
# routes = [
#   { pattern = "loudermx.com/artistas*", zone_name = "loudermx.com" }
# ]
